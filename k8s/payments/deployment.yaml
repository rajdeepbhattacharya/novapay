apiVersion: apps/v1
kind: Deployment
metadata:
  name: novapay-payments
  namespace: novapay
  labels:
    app: novapay-payments
    version: "2.1.0"
    tags.datadoghq.com/env: "demo"
    tags.datadoghq.com/service: "novapay-payments"
    tags.datadoghq.com/version: "2.1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: novapay-payments
  template:
    metadata:
      labels:
        app: novapay-payments
        tags.datadoghq.com/env: "demo"
        tags.datadoghq.com/service: "novapay-payments"
        tags.datadoghq.com/version: "2.1.0"
      annotations:
        ad.datadoghq.com/novapay-payments.logs: '[{"source":"python","service":"novapay-payments"}]'
    spec:
      containers:
      - name: novapay-payments
        image: novapay-payments:latest
        imagePullPolicy: Never  # Use local minikube image
        ports:
        - containerPort: 8001
        env:
        - name: DD_AGENT_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
        - name: DD_SERVICE
          value: "novapay-payments"
        - name: DD_ENV
          value: "demo"
        - name: DD_VERSION
          value: "2.1.0"
        - name: DD_LOGS_INJECTION
          value: "true"
        - name: DD_TRACE_SAMPLE_RATE
          value: "1.0"
        - name: DD_RUNTIME_METRICS_ENABLED
          value: "true"
        - name: DD_PROFILING_ENABLED
          value: "true"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 10
